'use client';

import DesignControls from '@/components/blog/design/DesignControls';

/** Blog design controls — writes themeSettings and applies via CSS variables. */
export default function BlogDesignPage() {
  return <DesignControls />;
}
