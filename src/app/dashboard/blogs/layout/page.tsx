'use client';

import LayoutBuilder from '@/components/blog/layout/LayoutBuilder';

/** Drag-and-drop builder for the public blog index/template layout. */
export default function BlogLayoutPage() {
  return <LayoutBuilder />;
}
